@page "/site"
@model WebsiteBuilder.Web.Pages.SiteModel
@{
    Layout = null;
}
@if (Model.Definition is null)
{
    @await Html.PartialAsync("_NoSiteHere")
}
else
{
    @await Html.PartialAsync("_RenderedSite", Model.Definition)
}
